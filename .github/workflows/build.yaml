---
name: 'Build'
on:
  pull_request: {}
  push:
    branches:
      - 'master'
jobs:
  image:
    runs-on: 'ubuntu-latest'
    strategy:
      matrix:
        version:
          - '1.12.19'
    steps:
      - uses: 'actions/checkout@v3'
        with:
          submodules: true
      - run: 'make FIRMWARE_VERSION=${{ matrix.version }}'
